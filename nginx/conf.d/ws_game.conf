# WebSocket del juego -> proceso PHP-CLI (game-ws:9001)
location /ws/game {
  proxy_pass         http://game-ws:9001;
  proxy_http_version 1.1;
  proxy_set_header   Upgrade $http_upgrade;
  proxy_set_header   Connection "upgrade";
  proxy_read_timeout 3600s;
}
