{
  "name": "final-project",
  "version": "1.0.0",
  "scripts": {
    "build": "node ./node_modules/tailwindcss/lib/cli.js -i ./src/styles/input.css -o ./dist/output.css",
    "start": "npm run build && serve . -l 3000",
    "dev": "npm run build && serve . -l 3000"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "serve": "^14.2.1"
  }
}
